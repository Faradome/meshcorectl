# meshcorectl command reference

Generated by `scripts/generate_command_reference.py` from each command's own
`--help` output — do not edit by hand, regenerate it instead.

## Contents

- [`meshcorectl`](#meshcorectl)
- [`meshcorectl advert`](#meshcorectl-advert)
- [`meshcorectl completion`](#meshcorectl-completion)
- [`meshcorectl config`](#meshcorectl-config)
- [`meshcorectl config current-context`](#meshcorectl-config-current-context)
- [`meshcorectl config delete-context`](#meshcorectl-config-delete-context)
- [`meshcorectl config get-contexts`](#meshcorectl-config-get-contexts)
- [`meshcorectl config set-context`](#meshcorectl-config-set-context)
- [`meshcorectl config use-context`](#meshcorectl-config-use-context)
- [`meshcorectl config view`](#meshcorectl-config-view)
- [`meshcorectl create`](#meshcorectl-create)
- [`meshcorectl create channel`](#meshcorectl-create-channel)
- [`meshcorectl create contact`](#meshcorectl-create-contact)
- [`meshcorectl delete`](#meshcorectl-delete)
- [`meshcorectl delete channel`](#meshcorectl-delete-channel)
- [`meshcorectl delete contact`](#meshcorectl-delete-contact)
- [`meshcorectl describe`](#meshcorectl-describe)
- [`meshcorectl describe contact`](#meshcorectl-describe-contact)
- [`meshcorectl describe device`](#meshcorectl-describe-device)
- [`meshcorectl exec`](#meshcorectl-exec)
- [`meshcorectl get`](#meshcorectl-get)
- [`meshcorectl get channel`](#meshcorectl-get-channel)
- [`meshcorectl get channels`](#meshcorectl-get-channels)
- [`meshcorectl get contact`](#meshcorectl-get-contact)
- [`meshcorectl get contacts`](#meshcorectl-get-contacts)
- [`meshcorectl get device`](#meshcorectl-get-device)
- [`meshcorectl get path`](#meshcorectl-get-path)
- [`meshcorectl get pending-contacts`](#meshcorectl-get-pending-contacts)
- [`meshcorectl get time`](#meshcorectl-get-time)
- [`meshcorectl login`](#meshcorectl-login)
- [`meshcorectl logout`](#meshcorectl-logout)
- [`meshcorectl logs`](#meshcorectl-logs)
- [`meshcorectl reboot`](#meshcorectl-reboot)
- [`meshcorectl scan`](#meshcorectl-scan)
- [`meshcorectl send`](#meshcorectl-send)
- [`meshcorectl send channel`](#meshcorectl-send-channel)
- [`meshcorectl send message`](#meshcorectl-send-message)
- [`meshcorectl set`](#meshcorectl-set)
- [`meshcorectl set device`](#meshcorectl-set-device)
- [`meshcorectl top`](#meshcorectl-top)
- [`meshcorectl top contact`](#meshcorectl-top-contact)
- [`meshcorectl trace`](#meshcorectl-trace)
- [`meshcorectl version`](#meshcorectl-version)

## `meshcorectl`

```
Usage: meshcorectl [OPTIONS] COMMAND [ARGS]...

  meshcorectl: a kubectl-style CLI for MeshCore companion radios.

Options:
  --context NAME                  Connection context to use (overrides the
                                  current context).
  -o, --output [table|wide|json|yaml|name]
                                  Output format.  [default: table]
  --timeout SECONDS               Per-command timeout (overrides the
                                  context/config default).
  -v, --verbose                   Increase logging verbosity (-v for info, -vv
                                  for debug). Login passwords, device PINs,
                                  and message text are kept out of the debug
                                  log.
  --config PATH                   Path to the meshcorectl config file
                                  (default:
                                  ~/.config/meshcorectl/config.yaml).
  --version                       Show the version and exit.
  --help                          Show this message and exit.

Commands:
  advert      Send an advertisement packet announcing this device.
  completion  Print a shell completion script for SHELL.
  config      Manage connection contexts (BLE/serial/TCP device profiles).
  create      Create a resource: import a contact, or define a channel.
  delete      Delete a contact, or clear a channel slot.
  describe    Show a detailed, human-readable view of one resource.
  exec        Run a raw console command on REPEATER (or every...
  get         Get one or more resources (device, contacts, channels, ...).
  login       Log into REPEATER (or room), or every -l/--selector match.
  logout      Log out of REPEATER (or room).
  logs        Show messages received by the device.
  reboot      Reboot the connected device.
  scan        List discoverable BLE and serial candidates.
  send        Send a message to a contact or a channel.
  set         Set a device parameter.
  top         Show telemetry (instant readings, or --history min/max/avg).
  trace       Trace the route through PATH, a comma-separated list of...
  version     Print the client version and, unless --client, the device's...
```

## `meshcorectl advert`

```
Usage: meshcorectl advert [OPTIONS]

  Send an advertisement packet announcing this device.

Options:
  --flood              Flood the advert instead of sending a normal (zero-hop)
                       one.
  --dry-run            Show what would be sent without sending it.
  -o, --output FORMAT  Output format for this command
                       (table|wide|json|yaml|name); overrides the global -o.
  --help               Show this message and exit.
```

## `meshcorectl completion`

```
Usage: meshcorectl completion [OPTIONS] {bash|zsh|fish}

  Print a shell completion script for SHELL.

  Load it for the current session:
    bash:  source <(meshcorectl completion bash)
    zsh:   source <(meshcorectl completion zsh)
    fish:  meshcorectl completion fish | source

  Or install it permanently, e.g. for bash:
    meshcorectl completion bash > ~/.local/share/bash-completion/completions/meshcorectl

Options:
  --help  Show this message and exit.
```

## `meshcorectl config`

```
Usage: meshcorectl config [OPTIONS] COMMAND [ARGS]...

  Manage connection contexts (BLE/serial/TCP device profiles).

Options:
  --help  Show this message and exit.

Commands:
  current-context  Print the name of the current context.
  delete-context   Delete context NAME.
  get-contexts     List all configured contexts.
  set-context      Create or update a connection context named NAME.
  use-context      Set the current context to NAME.
  view             Print the full contents of the config file.
```

## `meshcorectl config current-context`

```
Usage: meshcorectl config current-context [OPTIONS]

  Print the name of the current context.

Options:
  --help  Show this message and exit.
```

## `meshcorectl config delete-context`

```
Usage: meshcorectl config delete-context [OPTIONS] NAME

  Delete context NAME.

Options:
  --help  Show this message and exit.
```

## `meshcorectl config get-contexts`

```
Usage: meshcorectl config get-contexts [OPTIONS]

  List all configured contexts.

Options:
  --help  Show this message and exit.
```

## `meshcorectl config set-context`

```
Usage: meshcorectl config set-context [OPTIONS] NAME

  Create or update a connection context named NAME.

Options:
  --ble-address ADDR         BLE MAC address (or UUID on macOS).
  --ble-name SUBSTRING       Match a substring of the device's advertised BLE
                             name, when the address isn't known.
  --serial-port PATH         Serial device, e.g. /dev/ttyUSB0.
  --serial-baudrate INTEGER  [default: 115200]
  --tcp-host HOST            Hostname/IP of a TCP-exposed radio.
  --tcp-port INTEGER         [default: 5000]
  --timeout SECONDS          Default per-command timeout for this context.
  --current / --no-current   Also make this the current context (the first
                             context you create always becomes current).
  --help                     Show this message and exit.
```

## `meshcorectl config use-context`

```
Usage: meshcorectl config use-context [OPTIONS] NAME

  Set the current context to NAME.

Options:
  --help  Show this message and exit.
```

## `meshcorectl config view`

```
Usage: meshcorectl config view [OPTIONS]

  Print the full contents of the config file.

Options:
  -o, --output FORMAT  Output format for this command
                       (table|wide|json|yaml|name); overrides the global -o.
  --help               Show this message and exit.
```

## `meshcorectl create`

```
Usage: meshcorectl create [OPTIONS] COMMAND [ARGS]...

  Create a resource: import a contact, or define a channel.

Options:
  --help  Show this message and exit.

Commands:
  channel  Define channel INDEX with NAME and optional 32-hex-char KEY.
  contact  Import a contact from its meshcore:// card URI.
```

## `meshcorectl create channel`

```
Usage: meshcorectl create channel [OPTIONS] INDEX NAME [KEY]

  Define channel INDEX with NAME and optional 32-hex-char KEY.

  When KEY is omitted: a '#'-prefixed NAME is the public-channel convention,
  so the device derives the same secret from NAME itself -- anyone who knows
  the name can compute it and join. For any other NAME, meshcorectl generates
  a random secret for you (a name-derived secret would make a "private"
  channel just as guessable as a public one).

Options:
  --dry-run            Show what would be created without doing it.
  -o, --output FORMAT  Output format for this command
                       (table|wide|json|yaml|name); overrides the global -o.
  --help               Show this message and exit.
```

## `meshcorectl create contact`

```
Usage: meshcorectl create contact [OPTIONS]

  Import a contact from its meshcore:// card URI.

Options:
  --uri URI            A meshcore:// contact card URI.  [required]
  --dry-run            Show what would be imported without doing it.
  -o, --output FORMAT  Output format for this command
                       (table|wide|json|yaml|name); overrides the global -o.
  --help               Show this message and exit.
```

## `meshcorectl delete`

```
Usage: meshcorectl delete [OPTIONS] COMMAND [ARGS]...

  Delete a contact, or clear a channel slot.

Options:
  --help  Show this message and exit.

Commands:
  channel  Clear channel INDEX_OR_NAME (there's no true delete: the name...
  contact  Delete one contact by NAME, or every contact matching...
```

## `meshcorectl delete channel`

```
Usage: meshcorectl delete channel [OPTIONS] INDEX_OR_NAME

  Clear channel INDEX_OR_NAME (there's no true delete: the name and secret are
  cleared instead).

Options:
  --dry-run  Show what would be deleted without deleting it.
  --help     Show this message and exit.
```

## `meshcorectl delete contact`

```
Usage: meshcorectl delete contact [OPTIONS] NAME

  Delete one contact by NAME, or every contact matching -l/--selector.

Options:
  -l, --selector SELECTOR  Delete every contact matching this filter instead
                           of one by name, e.g. 't=client,u>30d'.
  --dry-run                Show what would be deleted without deleting it.
  --help                   Show this message and exit.
```

## `meshcorectl describe`

```
Usage: meshcorectl describe [OPTIONS] COMMAND [ARGS]...

  Show a detailed, human-readable view of one resource.

Options:
  --help  Show this message and exit.

Commands:
  contact  Describe one contact by name or public-key prefix.
  device   Describe the connected device.
```

## `meshcorectl describe contact`

```
Usage: meshcorectl describe contact [OPTIONS] NAME

  Describe one contact by name or public-key prefix.

Options:
  --help  Show this message and exit.
```

## `meshcorectl describe device`

```
Usage: meshcorectl describe device [OPTIONS]

  Describe the connected device.

Options:
  --help  Show this message and exit.
```

## `meshcorectl exec`

```
Usage: meshcorectl exec [OPTIONS] [REPEATER] -- CMD...

  Run a raw console command on REPEATER (or every -l/--selector match).

  Usage: meshcorectl exec REPEATER -- CMD...

  REPEATER and CMD are split out of a single catch-all argument (rather than
  two separate Click arguments) on purpose: Click fills fixed positionals in
  declaration order regardless of `required=False`, so an optional REPEATER
  ahead of a variadic CMD would silently steal CMD's first word whenever
  -l/--selector was used instead. `--` is stripped by Click itself wherever it
  appears, so it doesn't show up in `args`.

Options:
  -l, --selector SELECTOR  Run on every contact matching this filter instead
                           of one by name.
  --timeout SECONDS        How long to wait for the reply.  [default: 8.0]
  --dry-run                Show what would run without running it.
  --help                   Show this message and exit.
```

## `meshcorectl get`

```
Usage: meshcorectl get [OPTIONS] COMMAND [ARGS]...

  Get one or more resources (device, contacts, channels, ...).

Options:
  --help  Show this message and exit.

Commands:
  channel           Show one channel by index or name.
  channels          List every configured channel.
  contact           Show one contact by name or public-key prefix.
  contacts          List every contact (client/repeater/room/sensor)...
  device            Show the connected device's advertised info and...
  path              Show the routing path to a contact (flood, direct, or...
  pending-contacts  Watch for adverts from not-yet-added contacts, for...
  time              Show the device's current clock.
```

## `meshcorectl get channel`

```
Usage: meshcorectl get channel [OPTIONS] INDEX_OR_NAME

  Show one channel by index or name.

Options:
  -o, --output FORMAT  Output format for this command
                       (table|wide|json|yaml|name); overrides the global -o.
  --help               Show this message and exit.
```

## `meshcorectl get channels`

```
Usage: meshcorectl get channels [OPTIONS]

  List every configured channel.

  A device reserves a fixed number of channel slots (commonly 40) whether or
  not they're in use; empty slots are hidden by default -- same as `send
  channel`/`delete channel`, which never treat one as a valid target. Pass
  --all to include them too, in every output format.

Options:
  -A, --all            Include empty (unconfigured) channel slots.
  -o, --output FORMAT  Output format for this command
                       (table|wide|json|yaml|name); overrides the global -o.
  --help               Show this message and exit.
```

## `meshcorectl get contact`

```
Usage: meshcorectl get contact [OPTIONS] NAME

  Show one contact by name or public-key prefix.

Options:
  -o, --output FORMAT  Output format for this command
                       (table|wide|json|yaml|name); overrides the global -o.
  --help               Show this message and exit.
```

## `meshcorectl get contacts`

```
Usage: meshcorectl get contacts [OPTIONS]

  List every contact (client/repeater/room/sensor) known to the device.

Options:
  -l, --selector SELECTOR  Only list contacts matching this filter, e.g.
                           't=repeater,u<2d'.
  -o, --output FORMAT      Output format for this command
                           (table|wide|json|yaml|name); overrides the global
                           -o.
  --help                   Show this message and exit.
```

## `meshcorectl get device`

```
Usage: meshcorectl get device [OPTIONS]

  Show the connected device's advertised info and firmware/protocol query.

Options:
  -o, --output FORMAT  Output format for this command
                       (table|wide|json|yaml|name); overrides the global -o.
  --help               Show this message and exit.
```

## `meshcorectl get path`

```
Usage: meshcorectl get path [OPTIONS] NAME

  Show the routing path to a contact (flood, direct, or a hop list).

Options:
  -o, --output FORMAT  Output format for this command
                       (table|wide|json|yaml|name); overrides the global -o.
  --help               Show this message and exit.
```

## `meshcorectl get pending-contacts`

```
Usage: meshcorectl get pending-contacts [OPTIONS]

  Watch for adverts from not-yet-added contacts, for one timeout window.

  Unlike a long-lived session, a one-shot connection has no pre-existing cache
  of pending adverts to show: this actively listens for the current --timeout
  (or the context/config default) and reports whatever arrives in that window.
  Re-run (or pass a longer --timeout) to catch more.

Options:
  -o, --output FORMAT  Output format for this command
                       (table|wide|json|yaml|name); overrides the global -o.
  --help               Show this message and exit.
```

## `meshcorectl get time`

```
Usage: meshcorectl get time [OPTIONS]

  Show the device's current clock.

Options:
  -o, --output FORMAT  Output format for this command
                       (table|wide|json|yaml|name); overrides the global -o.
  --help               Show this message and exit.
```

## `meshcorectl login`

```
Usage: meshcorectl login [OPTIONS] REPEATER

  Log into REPEATER (or room), or every -l/--selector match.

Options:
  -l, --selector SELECTOR  Log into every contact matching this filter instead
                           of one by name.
  --password TEXT          Password (prefer --password-stdin, or the secure
                           prompt, over shell history).
  --password-stdin         Read the password from stdin.
  --dry-run                Show what would run without logging in.
  --help                   Show this message and exit.
```

## `meshcorectl logout`

```
Usage: meshcorectl logout [OPTIONS] REPEATER

  Log out of REPEATER (or room).

Options:
  --dry-run  Show what would run without logging out.
  --help     Show this message and exit.
```

## `meshcorectl logs`

```
Usage: meshcorectl logs [OPTIONS]

  Show messages received by the device.

Options:
  -f, --follow         Keep the connection open and print new messages as they
                       arrive.
  --since DURATION     Only show already-queued messages newer than this, e.g.
                       30m, 2h.
  --rx                 Follow the raw rx-log packet stream instead of messages
                       (requires --follow).
  -o, --output FORMAT  Output format for this command
                       (table|wide|json|yaml|name); overrides the global -o.
  --help               Show this message and exit.
```

## `meshcorectl reboot`

```
Usage: meshcorectl reboot [OPTIONS]

  Reboot the connected device.

Options:
  --yes                Confirm the reboot; required, since there is no
                       interactive prompt.
  --dry-run            Show what would happen without rebooting.
  -o, --output FORMAT  Output format for this command
                       (table|wide|json|yaml|name); overrides the global -o.
  --help               Show this message and exit.
```

## `meshcorectl scan`

```
Usage: meshcorectl scan [OPTIONS]

  List discoverable BLE and serial candidates.

Options:
  --ble / --no-ble        Include BLE devices.
  --serial / --no-serial  Include serial ports.
  --timeout SECONDS       BLE scan duration.  [default: 2.0]
  -o, --output FORMAT     Output format for this command
                          (table|wide|json|yaml|name); overrides the global
                          -o.
  --help                  Show this message and exit.
```

## `meshcorectl send`

```
Usage: meshcorectl send [OPTIONS] COMMAND [ARGS]...

  Send a message to a contact or a channel.

Options:
  --help  Show this message and exit.

Commands:
  channel  Send TEXT to CHANNEL (by index or name).
  message  Send TEXT to CONTACT, or to every contact matching -l/--selector.
```

## `meshcorectl send channel`

```
Usage: meshcorectl send channel [OPTIONS] CHANNEL TEXT

  Send TEXT to CHANNEL (by index or name).

Options:
  --dry-run  Show what would be sent without sending it.
  --help     Show this message and exit.
```

## `meshcorectl send message`

```
Usage: meshcorectl send message [OPTIONS] [CONTACT] TEXT

  Send TEXT to CONTACT, or to every contact matching -l/--selector.

  CONTACT and TEXT come out of a single catch-all argument, not two separate
  Click arguments: an optional leading CONTACT ahead of a required TEXT has
  the same Click positional-filling ambiguity `exec`'s docstring explains, so
  it's resolved the same way here. An unquoted multi-word message is joined
  back together with spaces.

Options:
  -l, --selector SELECTOR  Send to every contact matching this filter instead
                           of one by name.
  --wait-ack               Wait for delivery confirmation, retrying (and
                           flooding) automatically.
  --dry-run                Show what would be sent without sending it.
  --help                   Show this message and exit.
```

## `meshcorectl set`

```
Usage: meshcorectl set [OPTIONS] COMMAND [ARGS]...

  Set a device parameter.

Options:
  --help  Show this message and exit.

Commands:
  device  Set device parameter PARAM to VALUE.
```

## `meshcorectl set device`

```
Usage: meshcorectl set device [OPTIONS] PARAM VALUE

  Set device parameter PARAM to VALUE.

  Known parameters: advert-loc-policy, coords, manual-add-contacts, multi-acks, name, pin,
  telemetry-mode-base, telemetry-mode-env, telemetry-mode-loc, tx-power

Options:
  --dry-run            Show what would be set without setting it.
  -o, --output FORMAT  Output format for this command
                       (table|wide|json|yaml|name); overrides the global -o.
  --help               Show this message and exit.
```

## `meshcorectl top`

```
Usage: meshcorectl top [OPTIONS] COMMAND [ARGS]...

  Show telemetry (instant readings, or --history min/max/avg).

Options:
  --help  Show this message and exit.

Commands:
  contact  Request telemetry from a contact (typically a sensor).
```

## `meshcorectl top contact`

```
Usage: meshcorectl top contact [OPTIONS] NAME

  Request telemetry from a contact (typically a sensor).

Options:
  --history            Show min/max/avg history instead of instant readings.
  --since DURATION     History window for --history, e.g. 30m, 2h, 1d.
                       [default: 1h]
  -o, --output FORMAT  Output format for this command
                       (table|wide|json|yaml|name); overrides the global -o.
  --help               Show this message and exit.
```

## `meshcorectl trace`

```
Usage: meshcorectl trace [OPTIONS] PATH

  Trace the route through PATH, a comma-separated list of repeater public-key
  prefixes (e.g. "23,5f,3a").

Options:
  --timeout SECONDS    Override the device-suggested wait for the trace reply.
  -o, --output FORMAT  Output format for this command
                       (table|wide|json|yaml|name); overrides the global -o.
  --help               Show this message and exit.
```

## `meshcorectl version`

```
Usage: meshcorectl version [OPTIONS]

  Print the client version and, unless --client, the device's version.

Options:
  --client             Only print the client (this CLI's) version; don't
                       connect to a device.
  -o, --output FORMAT  Output format for this command
                       (table|wide|json|yaml|name); overrides the global -o.
  --help               Show this message and exit.
```
